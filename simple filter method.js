// Please create a function that takes in an array of numbers and returns a new array that only contains odd numbers. Review our last practice problem from last class if you need some ideas.

const num1 = [1, 2, 3, 4, 5, 6, 7]
const newArr = num1.filter(a => a % 3 === 0)
console.log(newArr)